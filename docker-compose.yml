services:
  url-shortener:
    build: .
    ports:
      - "3000:3000"
    environment:
      PORT: 3000
      BASE_URL: http://localhost:3000
      DB_PATH: /app/data/shortener.db
    volumes:
      - ./data:/app/data
